#version: '3.8'
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: warehouse_db
      POSTGRES_USER: awoni
      POSTGRES_PASSWORD: Awoniyi1.
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: